export const metadata = { title: "ALMOST OS - The OS for AI-Built Software" }
export default function RootLayout({ children }) {
  return <html><body style={{background:'#0a0a0a',color:'white',fontFamily:'monospace',margin:0}}>{children}</body></html>
}
